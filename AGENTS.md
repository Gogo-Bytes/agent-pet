# Agent Pet 工程规则

## 阶段性提交

每完成一个**可以独立验证、独立回滚、独立推送的工程阶段**，应：

1. 运行该阶段约定的测试、类型检查和构建验证；
2. 检查工作区和 diff，确认没有混入下一阶段或无关改动；
3. 创建清晰、单一目的的 Git commit；
4. 在用户允许且远程仓库已配置时推送该 commit；
5. 向用户报告阶段完成内容、验证命令、commit 和推送结果。

“一次对话”不等于“一个阶段”。只有完成了具备独立交付价值的阶段才提交；未完成的半成品、跨阶段耦合改动或仅文档草稿不强行提交。推送前不得执行破坏性 Git 操作，也不得覆盖用户已有改动。
