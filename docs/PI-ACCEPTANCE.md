# pi 迭代验收与当前范围

当前范围：只迭代 pi 流程。用户明确确认体验满意前，不推进 Claude Code、Codex 或其它 Agent 接入。

用户实测已反馈：
- 工作与完成气泡正常，点击完成气泡消失，新一轮任务再次显示。
- Session 名称更新正常。
- 桌面重启后连接恢复。
- 多 Session 独立展示。
- Esc 后气泡消失；这不证明所有重试/异常取消路径都已覆盖。
- `/reload` 后气泡消失；不将这一反馈扩大为全部身份保留及未读恢复语义已通过。

## 新增交互（待实机验收）

- 默认覆盖堆叠，显示最近更新的气泡及最多两层后方轮廓。
- 悬停或键盘聚焦展开全部通知列表，超出可用高度时滚动。
- 鼠标与焦点均离开后收起，不改变会话未读状态。
- 展开/收起不移动宠物，不改变现有 Agent 状态映射。
- 未展开时，原列表的透明空间不应阻挡其它应用。

需要重启桌面开发进程加载本次 Main/Preload 修改；保留 bridge 配置，不需要重启 pi。
